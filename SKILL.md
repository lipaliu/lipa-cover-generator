# 封面之王 · 生图智能体审美 Skill（King of Cover · Aesthetic Skill）

> 这份 Skill 是产品的**核心审美资产**。封面之王最终卖的不是"能生图"，而是这套
> **稳定产出高级感封面**的审美判断。任何对生图风格的调整，都应先改这份 Skill 与
> `server/design-matrix.js` 的权重，再让代码与 prompt 跟随。

---

## 1. 审美定位（必须遵守）

- **竖版（小红书 3:4 / 9:16 / 方形 1:1）**：严格保留用户原图与真实环境，**只做排版叠字 +
  Skill 指定的装饰**。**禁止**高概念 / 电影感 / 霓虹背景替换，禁止凭空换背景。
- **横版 / 宽版（16:9 / 4:3 / B 站安全框）**：可基于原图人物**大胆替换背景**为戏剧化
  高概念场景（霓虹赛博、暗调影棚、城市夜景大片、超现实概念空间）。已验证受认可方向：
  **霓虹款、撕纸彩色款**。
- **统一基线**：主标题巨大、与画面融合、可读性强；同一批每张必须明显不同；绝不生成
  虚假人脸 / 假人；绝不添加性暗示元素（口腔 / 舌苔 / 皮肤类一律按医学科普、口腔护理、
  美妆护理、生活方式处理）。

---

## 2. 七维设计矩阵 + 审美权重（核心）

设计 = 字体 A × 布局 B × 效果 C × 配色 D × 装饰 E × 构图 F × 氛围 G。

每个选项带 `weight`，控制加权随机被选中的概率，定义在 `server/design-matrix.js`：

| weight | 含义 | 出现概率 |
| --- | --- | --- |
| **3** | 优先 · 高级出片核心 | 最高 |
| **2** | 普通 · 安全可用 | 中 |
| **1** | 降权 · 易显廉价/少女气/风险高 | 很低 |
| **0** | 禁用 · 最易把图做丑 | 默认不出现 |

> weight=0 的选项**默认不会进入抽样池**，仅当用户关键词显式约束时才可能出现。

### 2.1 被禁用（weight=0）的"出丑高发区"

这些是真实出图反馈中把竖版做丑的元凶，已全部禁用：

| 维度 | 禁用项 | 原因 |
| --- | --- | --- |
| A 字体 | **A9 像素体 / A17 蜡笔质感 / A20 漫画体** | 廉价、幼稚、与变现主题冲突 |
| C 效果 | **C15 胶带粘贴** | DIY 廉价感 |
| E 装饰 | **E7 胶带贴纸 / E16 手绘涂鸦** | 廉价、杂乱 |

### 2.2 优先（weight=3）的"高级核心"

| 维度 | 优先项 |
| --- | --- |
| A 字体 | A1 书法、A2 行楷、A4 黑体、A7 衬线英文、A11 艺术装饰、A12 极细、A13 极粗、A14 霓虹、A15 飞白 |
| B 布局 | B1 居中霸屏、B2 左上对齐、B5 竖排、B8 底部横条、B10 中间腰封 |
| C 效果 | C1 纯色、C2 描边、C3 渐变、C4 立体、C5 霓虹、C9 金属、C14 撕纸 |
| D 配色 | D1 黑白、D2 黑金、D3 白金、D4 莫兰迪、D5 赛博霓虹、D7 海洋蓝、D12 同色渐变、D14 单色点缀、D18 冰蓝 |
| E 装饰 | E1 纯净、E2 几何框、E6 杂志拼贴、E9 撕纸、E11 渐变光晕、E15 数字编号、E18 噪点、E19 光线 |
| F 构图 | F1 全屏铺满、F6 中心聚焦、F7 留白呼吸、F9 黄金分割 |
| G 氛围 | G1 高级冷淡、G4 神秘暗黑、G7 未来科技、G9 奢华精致 |

### 2.3 降权（weight=1）的"低概率项"

A5 圆体、A6 手写、A19 打字机；C8 半透明、C11 涂鸦、C12 贴纸；
D16 明黄、D17 珊瑚粉、D20 樱花粉（少女气，与 IP 变现主题冲突）；
E8 邮票、E12 星星、E14 箭头、E17 花卉、E20 气泡；G10 幽默。

---

## 3. 加权抽样机制（实现说明）

`server/design-matrix.js`：

- **A / B / D 维度**：`weightedShuffle`（Efraimidis-Spirakis 加权不放回抽样），
  兼顾"同批尽量不重复"与"偏好高级项"。
- **C / E / F / G 维度**：`weightedPick`（加权随机），偏好高级项。
- weight=0 的项被 `filter` 排除，除非被用户关键词约束命中。

验证脚本：`node scripts/verify_matrix.mjs`（统计 2000 次抽样分布、确认禁用项零泄漏、
批量去重）。每次调整权重后都应重跑此脚本确认效果。

---

## 4. 调审美的标准动作（给后续维护者 / Claude Code）

1. 看一批真实出图，标记"丑"的样张及其 combination key（如 `A20+B7+C15+...`）。
2. 在 `server/design-matrix.js` 把对应选项 weight 下调（2→1 或 1→0）。
3. 把验证认可的高级风格 weight 上调（2→3）。
4. 运行 `node scripts/verify_matrix.mjs` 确认分布与零泄漏。
5. 同步更新本 `SKILL.md` 的 2.1 / 2.2 / 2.3 清单。
6. `git commit` 并推送，让审美资产持续沉淀。

---

## 5. 不变的硬规则（Prompt 层，见 `server/prompts.js`）

- 保留用户原图，只叠加文字与指定装饰，文字必须融入画面、不悬浮。
- 主标题巨大（占画面约 15~30%），主副标题字号对比 ≥ 3:1。
- 配色策略必须跟随底图分析（dominant color / brightness / subject position）。
- 竖版默认输出 1024×1536；B 站安全框出 16:9 且核心内容居中防裁切。
- 口腔 / 舌苔 / 皮肤 / 身体特写一律按非性暗示的医学科普 / 护理 / 生活方式内容处理。
