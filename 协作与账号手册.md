# 协作与账号手册（给 Codex / 团队 / 你自己）

> 这份是给非技术的人看的大白话说明。Claude、Codex、或者任何人改代码，都照这个来。

---

## 一、改代码的正确流程

不管用 Claude、Codex 还是别的工具，都是这四步：

1. **先拉最新**：`git pull`（把别人刚改的先同步下来）
2. **改代码**
3. **推上去**：`git add -A && git commit -m "说明改了啥" && git push`
4. 推到 GitHub 后，**Render 会自动部署**（约 1–2 分钟），刷新 https://bakabaka.onrender.com 就是新版本了。

### 三条铁律（避免踩坑）
- **改之前一定先 `git pull`**，别用旧版本盖掉别人刚推的东西。
- **别两个工具同时改同一批文件**。你让 Codex 改的时候，先别让 Claude 也在改，反过来也一样。谁改完推了，另一边先 `git pull` 再接着改。
- **出问题能回退**。每次 `commit` 都有记录，随时能退回到之前任何一个好版本（见第五节）。

---

## 二、要不要给 Codex（或别的工具）API 密钥？

**大多数情况：不用。**

- **只是改代码 + 推 GitHub 让 Render 部署 → 不需要密钥**（Render 那边有密钥，它负责跑）。这是最常见的用法。
- **只有想在"本地"把 app 跑起来测试时，才需要密钥**，密钥在项目根目录的 `.env.local` 文件里：
  - **Codex 就在这台 Mac、同一个文件夹里跑** → `.env.local` 已经在那儿了，直接能用，**啥都不用做**。
  - **Codex 在别处（云端 / 新下载的一份）** → 把 `.env.local` 这个文件**复制过去**，或把里面那几行贴到 Codex 的"环境变量 / Secrets"设置里。

> ⚠️ **千万别把 `.env.local` 传到 GitHub**（已经在 `.gitignore` 里忽略了，保持这样）。里面是 OpenAI / 火山的 API 密钥，泄露会被人盗刷。

---

## 三、账号一览

| 用户名 | 类型 | 能干啥 |
|---|---|---|
| **lipa** / **lipa2** | 管理员 | 全权限：无限、不限比例、**能进 /admin 后台** |
| **tongnan** | 会员 | 无限、不限比例、每比例最多 8 张，**进不了后台** |
| **guest** / **guest2** | 体验 | 只能小红书 3:4，单次最多 2 / 4 张 |

> 密码：`guest2 / lipa2 / tongnan` 的在 `render.yaml` 里；`lipa` 的在 Render 后台环境变量 `ACCESS_PASSWORD`。

---

## 四、怎么加 / 改账号

改 `render.yaml` 里的 **`ACCESS_ACCOUNTS_2`** 这一行，格式是 `用户名:密码:额度`，多个账号用空格隔开。额度三种写法：

- **数字**（如 `4`）= 体验账户，只能 3:4，单次最多这么多张。
- **`vip`** = 会员，无限、不限比例，但没有后台。
- **`admin`** = 管理员，全权限（含后台）。

例子：
```
guest2:baka2024:4  tongnan:12345677:vip  lipa2:bakaboss8:admin
```

改完 `git push`，Render 部署后即生效。

> 注意：Render 免费档的硬盘是"临时的"，在网页 /admin 后台手动加的账户，下次部署就没了。**所以加账户要改 `render.yaml`**（跟着 git 走才永久）。

---

## 五、出问题了怎么回退

每次改动都在 GitHub 有记录。想退回上一个好版本：

```bash
git log --oneline        # 看历史，找到那个好版本的编号（前 7 位）
git revert <编号>         # 生成一次"撤销"提交（推荐，安全）
git push
```

不确定就别自己动手——把"我想退回到 XXX 那次"告诉 Claude 或 Codex，让它帮你退。

---

## 六、一句话总结

**改前先 `pull`、别同时改、改完 `push` 让 Render 自动上线；纯改代码不用密钥；加账户改 `render.yaml`。**
